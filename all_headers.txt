===== /obc/include/drivers/adc_driver.h =====
#ifndef DRIVERS_ADC_DRIVER_H
#define DRIVERS_ADC_DRIVER_H

#include <stdint.h>

typedef struct {
    int32_t (*read)(void);
} SensorDriver;

struct AppState;

SensorDriver *adc_sensor_driver(void);
void adc_driver_bind_state(struct AppState *state);
double adc_scale_voltage(int32_t voltage_raw);
int32_t adc_read_impl(void);

#endif

===== /obc/include/drivers/adcs_router.h =====
#ifndef DRIVERS_ADCS_ROUTER_H
#define DRIVERS_ADCS_ROUTER_H

int adcs_router_window(void);

#endif

===== /obc/include/drivers/imu_mux.h =====
#ifndef DRIVERS_IMU_MUX_H
#define DRIVERS_IMU_MUX_H

int imu_mux_channel(void);

#endif

===== /obc/include/drivers/radio_udp.h =====
#ifndef DRIVERS_RADIO_UDP_H
#define DRIVERS_RADIO_UDP_H

#include "flight/state.h"

int radio_open(AppState *state);
int radio_send_packet(AppState *state, const TelemetryPacket *packet);
void radio_close(AppState *state);

#endif

===== /obc/include/drivers/sensor_bus.h =====
#ifndef DRIVERS_SENSOR_BUS_H
#define DRIVERS_SENSOR_BUS_H

int sensor_bus_settle_cycles(void);

#endif

===== /obc/include/drivers/telemetry_ingest.h =====
#ifndef DRIVERS_TELEMETRY_INGEST_H
#define DRIVERS_TELEMETRY_INGEST_H

#include "flight/state.h"

void fsw_tm_push(AppState *state, const TelemetryFrame *frame);

#endif

===== /obc/include/drivers/temp_copy.h =====
#ifndef DRIVERS_TEMP_COPY_H
#define DRIVERS_TEMP_COPY_H

#include "flight/state.h"

void temp_guarded_copy(AppState *state, int16_t temp);

#endif

===== /obc/include/drivers/thermal_sensor.h =====
#ifndef DRIVERS_THERMAL_SENSOR_H
#define DRIVERS_THERMAL_SENSOR_H

#include <stdint.h>
#include "flight/state.h"

int16_t thermal_model_sample(uint32_t tick);
void sensor_buffer_refresh(AppState *state);
void thermal_monitor_update(AppState *state);

#endif

===== /obc/include/flight/fault.h =====
#ifndef FLIGHT_FAULT_H
#define FLIGHT_FAULT_H

#include "flight/state.h"

void flight_fault_init(AppState *state);
void flight_fault_check_dispatch(void);
void fault_check_invoke(AppState *state);

#endif

===== /obc/include/flight/fdir_notes.h =====
#ifndef FLIGHT_FDIR_NOTES_H
#define FLIGHT_FDIR_NOTES_H

int fdir_note_slots(void);

#endif

===== /obc/include/flight/mission.h =====
#ifndef FLIGHT_MISSION_H
#define FLIGHT_MISSION_H

const char *mission_phase_name(void);

#endif

===== /obc/include/flight/power.h =====
#ifndef FLIGHT_POWER_H
#define FLIGHT_POWER_H

#include "flight/state.h"

double solar_input_for_tick(uint32_t tick);
void power_update(AppState *state);

#endif

===== /obc/include/flight/safe_mode.h =====
#ifndef FLIGHT_SAFE_MODE_H
#define FLIGHT_SAFE_MODE_H

#include "flight/state.h"

void safe_mode_maybe_enter(AppState *state);
void solar_deploy_sequence(void);

#endif

===== /obc/include/flight/state.h =====
#ifndef FLIGHT_STATE_H
#define FLIGHT_STATE_H

#include <stdbool.h>
#include <stdint.h>
#include <stddef.h>
#include <stdio.h>
#include <netinet/in.h>

#define TICKS_PER_ORBIT 150
#define TOTAL_ORBITS 15
#define TOTAL_TICKS (TICKS_PER_ORBIT * TOTAL_ORBITS)
#define QUEUE_SIZE 1024
#define ADC_SHIFT 4
#define MAX_TEMP 90
#define SENSOR_COPY_DEST 16
#define THERMAL_WRAP_INCREMENT 38
#define THERMAL_WRAP_INITIAL 950
#define TICK_DELAY_MS 50
#define TEMP_CRITICAL 85.0
#define VIB_CRITICAL 12.0

typedef enum {
    TASK_PRIO_HIGH = 0,
    TASK_PRIO_MEDIUM = 1,
    TASK_PRIO_LOW = 2
} TaskPriority;

typedef struct {
    uint32_t tick;
    int32_t sensor_x;
    int32_t sensor_y;
    int32_t sensor_z;
    uint64_t timestamp_ns;
} TelemetryFrame;

typedef struct {
    uint32_t tick;
    double battery_voltage;
    int32_t temperature_raw;
    uint16_t crc;
} WireTelemetry;

typedef struct {
    uint32_t tick;
    int16_t temperature_raw;
    double battery_voltage;
    uint16_t crc;
} TelemetryPacket;

typedef struct {
    int16_t current_raw;
    int16_t previous_raw;
    uint32_t last_tick;
} ThermalBuffer;

typedef struct {
    double angle;
    bool locked;
} SolarPanelConfig;

typedef struct __attribute__((packed)) {
    TelemetryFrame queue[QUEUE_SIZE];
    uint8_t reserved[sizeof(TelemetryFrame) - sizeof(void *)];
    void (*fault_check_fn)(void);
} SharedMemoryRegion;

struct AppState;

typedef struct {
    uint16_t runtime_ms;
} ThermalTaskState;

typedef struct {
    uint32_t runtime_ms;
} RefreshTaskState;

typedef struct {
    const char *name;
    uint16_t period;
    TaskPriority priority;
    uint16_t run_count;
    uint16_t runtime_ms;
    void (*task_fn)(struct AppState *state);
} TaskRecord;

typedef struct AppState {
    uint32_t tick;
    uint32_t telemetry_frames;
    uint32_t packets_sent;
    uint32_t orbit_index;
    bool safe_mode;
    bool therm_stale;
    bool thermal_wrapped;
    bool copy_fault_seen;
    bool trap_triggered;
    bool crc_bad_seen;
    bool scheduler_bias_flipped;
    bool command_pending;

    int32_t adc_raw;
    double adc_voltage_reported;
    double battery_voltage_true;
    double battery_voltage_reported;
    int16_t current_temperature;
    int16_t logged_temperature;
    int16_t sensor_copy_canary;
    ThermalBuffer thermal_buffer;
    TelemetryPacket last_packet;
    SharedMemoryRegion shared;
    TelemetryFrame *telemetry_cursor;
    ThermalTaskState thermal_state;
    RefreshTaskState refresh_state;
    TaskRecord *tasks;
    size_t task_count;
    FILE *ground_log;
    int udp_socket;
    struct sockaddr_in ground_addr;
    void (*fault_dispatch)(void);
    double target_heater_pwm;    // 0 to 100
    double target_wheel_rpm;     // 0 to 5000
    double actual_temperature;
    double actual_wheel_rpm;
    double vibration_amplitude;
} AppState;

#endif

===== /obc/include/flight/status.h =====
#ifndef FLIGHT_STATUS_H
#define FLIGHT_STATUS_H

int flight_status_word(void);

#endif

===== /obc/include/flight/tasks.h =====
#ifndef FLIGHT_TASKS_H
#define FLIGHT_TASKS_H

#include "flight/state.h"

void telemetry_ingest_task(AppState *state);
void adc_sample_task(AppState *state);
void power_update_task(AppState *state);
void sensor_refresh_task(AppState *state);
void thermal_monitor_task(AppState *state);
void fault_check_task(AppState *state);
void comms_downlink_task(AppState *state);
void command_processing_task(AppState *state);

#endif

===== /obc/include/hal/board.h =====
#ifndef HAL_BOARD_H
#define HAL_BOARD_H

int hal_board_revision(void);

#endif

===== /obc/include/hal/gpio_matrix.h =====
#ifndef HAL_GPIO_MATRIX_H
#define HAL_GPIO_MATRIX_H

int hal_gpio_matrix_rows(void);

#endif

===== /obc/include/hal/power_rail.h =====
#ifndef HAL_POWER_RAIL_H
#define HAL_POWER_RAIL_H

double hal_power_trim(void);

#endif

===== /obc/include/hal/timers.h =====
#ifndef HAL_TIMERS_H
#define HAL_TIMERS_H

unsigned int hal_timer_divider(void);

#endif

===== /obc/include/hal/watchdog.h =====
#ifndef HAL_WATCHDOG_H
#define HAL_WATCHDOG_H

int hal_watchdog_window(void);

#endif

===== /obc/include/logging/cache.h =====
#ifndef LOGGING_CACHE_H
#define LOGGING_CACHE_H

int logging_cache_depth(void);

#endif

===== /obc/include/logging/format.h =====
#ifndef LOGGING_FORMAT_H
#define LOGGING_FORMAT_H

const char *logging_format_name(void);

#endif

===== /obc/include/logging/log.h =====
#ifndef LOGGING_LOG_H
#define LOGGING_LOG_H

#include "flight/state.h"

void log_tick_line(const AppState *state);

#endif

===== /obc/include/middleware/command_queue.h =====
#ifndef MIDDLEWARE_COMMAND_QUEUE_H
#define MIDDLEWARE_COMMAND_QUEUE_H

struct AppState;

int command_queue_depth(void);
void cmd_bind_state(struct AppState *state);
void cmd_set_actuators(double temp_pwm, double wheel_rpm);

#endif

===== /obc/include/middleware/crc16.h =====
#ifndef MIDDLEWARE_CRC16_H
#define MIDDLEWARE_CRC16_H

#include <stddef.h>
#include <stdint.h>

uint16_t crc16_ccitt(const uint8_t *data, size_t len);

#endif

===== /obc/include/middleware/frame_codec.h =====
#ifndef MIDDLEWARE_FRAME_CODEC_H
#define MIDDLEWARE_FRAME_CODEC_H

unsigned int frame_codec_magic(void);

#endif

===== /obc/include/middleware/packet_router.h =====
#ifndef MIDDLEWARE_PACKET_ROUTER_H
#define MIDDLEWARE_PACKET_ROUTER_H

unsigned int packet_router_ports(void);

#endif

===== /obc/include/middleware/telemetry_pipeline.h =====
#ifndef MIDDLEWARE_TELEMETRY_PIPELINE_H
#define MIDDLEWARE_TELEMETRY_PIPELINE_H

#include "flight/state.h"

void packet_prepare(AppState *state);
void packet_units_adjust(TelemetryPacket *packet);
WireTelemetry packet_to_wire(const TelemetryPacket *packet);

#endif

===== /obc/include/obc_physics.h =====
#ifndef OBC_PHYSICS_H
#define OBC_PHYSICS_H

double orbital_period(void);
double solar_incidence(int tick);
int verify_flag(unsigned int input);

#endif

===== /obc/include/physics/attitude_hint.h =====
#ifndef PHYSICS_ATTITUDE_HINT_H
#define PHYSICS_ATTITUDE_HINT_H

double attitude_hint_bias(void);

#endif

===== /obc/include/physics/orbit_model.h =====
#ifndef PHYSICS_ORBIT_MODEL_H
#define PHYSICS_ORBIT_MODEL_H

double orbit_model_drag_term(void);

#endif

===== /obc/include/physics/physics_glue.h =====
#ifndef PHYSICS_PHYSICS_GLUE_H
#define PHYSICS_PHYSICS_GLUE_H

struct AppState;

double physics_orbital_period(void);
double physics_solar_incidence(int tick);
int physics_verify_flag(unsigned int input);
void physics_update_actuators(struct AppState *state, int current_tick);

#endif

===== /obc/include/rtos/scheduler.h =====
#ifndef RTOS_SCHEDULER_H
#define RTOS_SCHEDULER_H

#include "flight/state.h"

void scheduler_init(AppState *state);
void scheduler_run_tick(AppState *state);
TaskRecord *scheduler_find_task(AppState *state, const char *name);

#endif

===== /obc/include/utils/allocator_guard.h =====
#ifndef UTILS_ALLOCATOR_GUARD_H
#define UTILS_ALLOCATOR_GUARD_H

#include <stddef.h>

void checked_free(void *ptr);
void allocator_guard_set_static_target(void *ptr);

#endif

===== /obc/include/utils/checksum.h =====
#ifndef UTILS_CHECKSUM_H
#define UTILS_CHECKSUM_H

unsigned int rolling_checksum_seed(void);

#endif

===== /obc/include/utils/env.h =====
#ifndef UTILS_ENV_H
#define UTILS_ENV_H


#endif

===== /obc/include/utils/inspect.h =====
#ifndef UTILS_INSPECT_H
#define UTILS_INSPECT_H

int inspect_window_stride(void);

#endif

===== /obc/include/utils/mathx.h =====
#ifndef UTILS_MATHX_H
#define UTILS_MATHX_H

double clamp_unit_interval(double value);

#endif

===== /obc/include/utils/window_stats.h =====
#ifndef UTILS_WINDOW_STATS_H
#define UTILS_WINDOW_STATS_H

int window_stats_span(void);

#endif

